<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>H2o on SIS Lab</title>
    <link>https://meganii.com/tags/h2o/</link>
    <description>Recent content in H2o on SIS Lab</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>ja</language>
    <lastBuildDate>Mon, 18 Jul 2016 21:44:52 +0900</lastBuildDate>
    <atom:link href="https://meganii.com/tags/h2o/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>さくらVPSのCentOS6.4にh2oをAnsibleでビルド＆インストール</title>
      <link>https://meganii.com/blog/2016/07/18/installing-h2o-with-ansible/</link>
      <pubDate>Mon, 18 Jul 2016 21:44:52 +0900</pubDate>
      
      <guid>https://meganii.com/blog/2016/07/18/installing-h2o-with-ansible/</guid>
      <description>

&lt;p&gt;前回&lt;a href=&#34;https://meganii.com/blog/2016/07/17/install-h2load/&#34;&gt;http/2ベンチマークツール「h2load」をインストール - SIS Lab&lt;/a&gt;でhttp/2ベンチマークツールをインストールしたので、いよいよ&lt;code&gt;h2o&lt;/code&gt;をインストールします。&lt;/p&gt;

&lt;p&gt;さくらVPSで運用しているCentOSは、Ansible経由で整備しているので今回もAnsibleのタスクを作成して、まずはローカルの開発環境でテスト、そしてさくらVPSのCentOSに適用します。&lt;/p&gt;

&lt;p&gt;Wordpressも、&lt;code&gt;h2o&lt;/code&gt;で動かそうと思ったのですが、うまくリソースが取得できませんでした。下記の設定では、静的ジェネレータ&lt;code&gt;hugo&lt;/code&gt;で作成するファイルだけ&lt;code&gt;h2o&lt;/code&gt;で公開するときの設定です。&lt;/p&gt;

&lt;div class=&#34;google-adsense&#34;&gt;
    &lt;p&gt;スポンサーリンク&lt;/p&gt;
    &lt;script async src=&#34;//pagead2.googlesyndication.com/pagead/js/adsbygoogle.js&#34;&gt;&lt;/script&gt;
    
    &lt;ins class=&#34;adsbygoogle&#34;
         style=&#34;display:block&#34;
         data-ad-client=&#34;ca-pub-5340212676785058&#34;
         data-ad-slot=&#34;7870420352&#34;
         data-ad-format=&#34;auto&#34;&gt;&lt;/ins&gt;
    &lt;script&gt;
    (adsbygoogle = window.adsbygoogle || []).push({});
    &lt;/script&gt;
&lt;/div&gt;


&lt;h2 id=&#34;ansibleでのh2oインストール方針&#34;&gt;Ansibleでのh2oインストール方針&lt;/h2&gt;

&lt;ul&gt;
&lt;li&gt;ダウンロードしたソースコードは、&lt;code&gt;/tmp&lt;/code&gt;に格納する&lt;/li&gt;
&lt;li&gt;実行ファイルは&lt;code&gt;/usr/local/bin&lt;/code&gt;に格納する&lt;/li&gt;
&lt;li&gt;h2oの設定ファイルは&lt;code&gt;/usr/local/etc/h2o/conf/&lt;/code&gt;に格納する&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&#34;ansibleのロール設定&#34;&gt;Ansibleのロール設定&lt;/h2&gt;

&lt;pre&gt;&lt;code&gt;group_vars
roles
  - h2o
    - tasks
    - templates
hosts
site.yml
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;tasks-main-yml&#34;&gt;tasks/main.yml&lt;/h3&gt;

&lt;pre&gt;&lt;code&gt;---
- name: install cmake
  yum: name=cmake state=present
  sudo: yes

- name: install package
  yum: pkg={{ item }} state=present enablerepo=epel
  with_items:
    - openssl
    - openssl-devel
  sudo: yes

- name: check h2o installed
  command: test -x /usr/local/bin/h2o
  register: h2o_present
  ignore_errors: yes
  changed_when: false

- name: download h2o
  get_url: url=https://github.com/h2o/h2o/archive/v{{h2o_version}}.tar.gz dest=/tmp/h2o.tgz
  when: h2o_present|failed

- name: expand h2o
  command: tar xzf h2o.tgz chdir=/tmp creates=/tmp/h2o-{{h2o_version}}
  when: h2o_present|failed

- name: cmake h2o
  command: cmake -DWITH_BUNDLED_SSL=on . chdir=/tmp/h2o-{{h2o_version}} creates=/tmp/h2o-{{h2o_version}}/Makefile
  when: h2o_present|failed

- name: make h2o
  command: make chdir=/tmp/h2o-{{h2o_version}} creates=/tmp/h2o-{{h2o_version}}/h2o
  when: h2o_present|failed

- name: install h2o
  command: make install chdir=/tmp/h2o-{{h2o_version}} creates=/usr/local/bin/h2o
  when: h2o_present|failed
  sudo: yes

- name: Ensures conf dir exists
  file: path=/usr/local/etc/h2o/conf/ state=directory

- name: Copy h2o configuration
  template: src=h2o.conf dest=/usr/local/etc/h2o/conf/h2o.conf

- name: Ensures log dir exists
  file: path=/var/log/h2o/ state=directory

- name: Create access log file
  file: path=/var/log/h2o/access.log state=touch

- name: Create error log file
  file: path=/var/log/h2o/error.log state=touch

- name: Create pid log file
  file: path=/usr/local/etc/h2o/h2o.pid state=touch mode=0777

- name: add iptables rule for http
  lineinfile: dest=/etc/sysconfig/iptables regexp=&amp;quot;{{ httpd_port }}&amp;quot; line=&amp;quot;-A RH-Firewall-1-INPUT -m state --state NEW -m tcp -p tcp --dport {{ httpd_port }} -j ACCEPT&amp;quot; insertbefore=&amp;quot;^# add end&amp;quot; state=present

- name: add iptables rule for https
  lineinfile: dest=/etc/sysconfig/iptables regexp=&amp;quot;{{ https_port }}&amp;quot; line=&amp;quot;-A RH-Firewall-1-INPUT -m state --state NEW -m tcp -p tcp --dport {{ https_port }} -j ACCEPT&amp;quot; insertbefore=&amp;quot;^# add end&amp;quot; state=present

- name: iptables restart
  command: /etc/init.d/iptables restart
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;templates-h2o-conf&#34;&gt;templates/h2o.conf&lt;/h3&gt;

&lt;pre&gt;&lt;code&gt;access-log: /var/log/h2o/access.log
error-log:  /var/log/h2o/error.log
pid-file:   /usr/local/etc/h2o/h2o.pid

http2-reprioritize-blocking-assets: ON
gzip: ON
expires: 1 day

hosts:
  &amp;quot;meganii.com:80&amp;quot;:
    listen:
      port: 80
    paths:
      &amp;quot;/&amp;quot;:
        redirect: https://meganii.com/
  &amp;quot;meganii.com:443&amp;quot;:
    listen:
      port: 443
      ssl:
        certificate-file: /etc/letsencrypt/live/meganii.com/fullchain.pem
        key-file:         /etc/letsencrypt/live/meganii.com/privkey.pem
        minimum-version: TLSv1
        cipher-suite: &amp;quot;AESGCM:HIGH:!3DES:!RSA:!aNULL:!eNULL:!MD5&amp;quot;
        cipher-preference: server
    paths:
      &amp;quot;/&amp;quot;:
        file.dir: /home/web/www/meganii.com/
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;h2oでwordpressを動かそうとしたときのメモ&#34;&gt;h2oでWordpressを動かそうとしたときのメモ&lt;/h2&gt;

&lt;p&gt;&lt;code&gt;h2o&lt;/code&gt;と&lt;code&gt;php-fpm&lt;/code&gt;の設定を下記の通り設定すれば、h2oでWordpressが動くことは動きました。でも、以下の不具合が出てしまい、うまく表示ができない状態になってしまいました。&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;静的ファイル(css,js,img)の読込エラーで、レイアウトが崩れる&lt;/li&gt;
&lt;li&gt;サブディレクトリで複数のブログを管理しているが、その管理画面の表示がうまくできない&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;静的ファイルの読込エラーは、おそらくh2oとWordpressのファイル読取・実行権限が上手くいっていないことが考えられます。h2oはデフォルトでnobodyとして動くはずなので、&lt;code&gt;user: wordpress&lt;/code&gt;としたのですが、ダメでした。どこを見なおせばよいのか。。&lt;/p&gt;

&lt;p&gt;また、管理画面の表示がうまくいないのは、nginxでいうところのRewriteの部分が影響しているとかんがえられる。これは、h2oでどのように書くのがよいのか要調査。&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;# Rewrite multisite &#39;.../wp-.*&#39; and &#39;.../*.php&#39;.
if (!-e $request_filename) {
        rewrite ^/[_0-9a-zA-Z-]+(/wp-.*) $1 last;
        rewrite ^/[_0-9a-zA-Z-]+.*(/wp-admin/.*\.php)$ $1 last;
        rewrite ^/[_0-9a-zA-Z-]+(/.*\.php)$ $1 last;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;h2o-conf&#34;&gt;h2o.conf&lt;/h3&gt;

&lt;pre&gt;&lt;code&gt;user: wordpress
file.custom-handler:
  extension: .php
  fastcgi.connect:
    port: /var/run/php-fpm/wordpress.sock
    type: unix
&amp;quot;domain.com:80&amp;quot;:
  listen:
    port: 80
  paths:
    &amp;quot;/&amp;quot;:
      redirect: https://domain.com/
&amp;quot;domain.com:443&amp;quot;:
  listen:
    port: 443
    ssl:
      certificate-file: /etc/letsencrypt/live/domain.com/fullchain.pem
      key-file:         /etc/letsencrypt/live/domain.com/privkey.pem
      minimum-version: TLSv1
      cipher-suite: &amp;quot;AESGCM:HIGH:!3DES:!RSA:!aNULL:!eNULL:!MD5&amp;quot;
      cipher-preference: server
  paths:
    &amp;quot;/&amp;quot;:
      file.dir: /srv/wordpress
      file.dirlisting: OFF
      redirect:
        url: /index.php/
        internal: YES
        status: 307
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;etc-php-fpm-d-wordpress-conf&#34;&gt;/etc/php-fpm.d/wordpress.conf&lt;/h3&gt;

&lt;pre&gt;&lt;code&gt;[wordpress]
listen = /var/run/php-fpm/wordpress.sock
listen.owner = nginx
listen.group = nginx
listen.mode = 0660

user = wordpress
group = wordpress

pm = dynamic
pm.max_children = 10
pm.start_servers = 1
pm.min_spare_servers = 1
pm.max_spare_servers = 3
pm.max_requests = 500
chdir = /srv/wordpress/
php_admin_value[open_basedir] = /srv/wordpress/:/tmp
php_admin_value[error_log] = /var/log/php-fpm/www-error.log
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;まとめ&#34;&gt;まとめ&lt;/h3&gt;

&lt;p&gt;静的ファイルをホストするには、configファイルもシンプルで楽、たぶんデフォルトでうまいことやってくれるのが&lt;code&gt;h2o&lt;/code&gt;だと思う。Wordpressをなんとか動かせるようにして、サーバープッシュなどの機能を有効にしてベンチマークを取りたい。&lt;/p&gt;

&lt;p&gt;ただ、やりたいことはただ単純にブログと簡単なアプリを公開したいだけなので、その目的を実現するためには、ネットで情報があるnginx + Wordpressの方が設定を探しやすいことは探しやすい。&lt;/p&gt;

&lt;p&gt;まあ、何事も勉強ということで。&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>http/2ベンチマークツール「h2load」をインストール</title>
      <link>https://meganii.com/blog/2016/07/17/install-h2load/</link>
      <pubDate>Sun, 17 Jul 2016 20:19:21 +0900</pubDate>
      
      <guid>https://meganii.com/blog/2016/07/17/install-h2load/</guid>
      <description>

&lt;p&gt;&lt;img src=&#34;https://farm6.staticflickr.com/5624/21367186743_cbcb0e5268_z.jpg?zz=1&#34; alt=&#34;4 Good Reasons for Using HTTP/2&#34; title=&#34;4 Good Reasons for Using HTTP/2&#34; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;https://www.flickr.com/photos/86979666@N00/21367186743/&#34; title=&#34;4 Good Reasons for Using HTTP/2 / Tsahi Levent-Levi&#34;&gt;4 Good Reasons for Using HTTP/2 / Tsahi Levent-Levi&lt;/a&gt;&lt;/p&gt;

&lt;div class=&#34;google-adsense&#34;&gt;
    &lt;p&gt;スポンサーリンク&lt;/p&gt;
    &lt;script async src=&#34;//pagead2.googlesyndication.com/pagead/js/adsbygoogle.js&#34;&gt;&lt;/script&gt;
    
    &lt;ins class=&#34;adsbygoogle&#34;
         style=&#34;display:block&#34;
         data-ad-client=&#34;ca-pub-5340212676785058&#34;
         data-ad-slot=&#34;7870420352&#34;
         data-ad-format=&#34;auto&#34;&gt;&lt;/ins&gt;
    &lt;script&gt;
    (adsbygoogle = window.adsbygoogle || []).push({});
    &lt;/script&gt;
&lt;/div&gt;


&lt;p&gt;今、このブログは静的サイトジェネレータ&lt;code&gt;hugo&lt;/code&gt;で作成したhtmlファイルを、&lt;code&gt;nginx&lt;/code&gt;で公開しています。また、別のサイトは、&lt;code&gt;nginx&lt;/code&gt;+&lt;code&gt;wordpress&lt;/code&gt;で運営しています。&lt;/p&gt;

&lt;p&gt;今気になっているのは、&lt;code&gt;h2o&lt;/code&gt;と呼ばれているWebサーバです。&lt;code&gt;nginx&lt;/code&gt;から&lt;code&gt;h2o&lt;/code&gt;に変えた場合、どのぐらい早くなるのか？そもそも、今の段階でどのぐらいのリクエストを捌けているのか？という疑問を解消するために、まずはベンチマークを取ろうと、ベンチマークツールを整備しました。&lt;/p&gt;

&lt;h2 id=&#34;各種ベンチマークツール&#34;&gt;各種ベンチマークツール&lt;/h2&gt;

&lt;ul&gt;
&lt;li&gt;ab&lt;/li&gt;
&lt;li&gt;wrk&lt;/li&gt;
&lt;li&gt;h2load&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;真っ先に思い浮かぶのは、&lt;code&gt;ab&lt;/code&gt;コマンドです。昔もapacheとnginxの比較をしようとしたときに使いました。&lt;a href=&#34;https://meganii.com/blog/2013/05/27/benchmark-apache/&#34;&gt;abコマンド実行時のエラーを回避する - SIS Lab&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;ab&lt;/code&gt;コマンドよりも、モダンなのは、&lt;code&gt;wrk&lt;/code&gt;らしいです。&lt;a href=&#34;https://siguniang.wordpress.com/2015/06/21/notes-on-wrk-http-benchmarking-tool/&#34;&gt;HTTP ベンチマークツール wrk についてメモ | Siguniang&amp;rsquo;s Blog&lt;/a&gt;
&lt;a href=&#34;https://github.com/wg/wrk&#34;&gt;GitHub - wg/wrk: Modern HTTP benchmarking tool&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;wrk&lt;/code&gt;も便利そうなのですが、http/2プロトコルに対応していません。&lt;/p&gt;

&lt;p&gt;&lt;a href=&#34;http://hb.matsumoto-r.jp/entry/2016/01/14/114048&#34;&gt;Webサーバのベンチマークツールはh2loadが便利 - 人間とウェブの未来&lt;/a&gt;
&lt;a href=&#34;http://d.hatena.ne.jp/kazu-yamamoto/20151225/1451287148&#34;&gt;h2load を使おう - あどけない話&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;上記のサイトを参考に&lt;code&gt;h2load&lt;/code&gt;が便利そうなので、インストールしてみます。brewともyumでもインストールはできないみたいなので、自前でビルドします。&lt;/p&gt;

&lt;h2 id=&#34;h2load-のインストール&#34;&gt;&lt;code&gt;h2load&lt;/code&gt;のインストール&lt;/h2&gt;

&lt;p&gt;まず、ローカルのMacにインストールを試してみたのですが、うまくビルドまで辿り着きませんでした。次に、VirtualBoxのCentOS6.4に入れてみようとしたのですが、同じく行き詰まり、結局Ubuntuの環境を作ってインストールしました。&lt;/p&gt;

&lt;p&gt;メモ書きと共に残しておきますが、的はずれなことを書いているかもしれないので注意してください。&lt;/p&gt;

&lt;h2 id=&#34;ubuntu-server-14-04を-vagrantで構築&#34;&gt;Ubuntu Server 14.04を vagrantで構築&lt;/h2&gt;

&lt;p&gt;&lt;a href=&#34;https://atlas.hashicorp.com/boxes/search&#34;&gt;Discover Vagrant Boxes | Atlas by HashiCorp&lt;/a&gt;でUbuntu Server 14.04を探して、vagrantからプロビジョニングします。&lt;/p&gt;

&lt;p&gt;Official Ubuntu Server 14.04 LTS (Trusty Tahr) builds&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;vagrant init ubuntu/trusty64; vagrant up --provider virtualbox
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;ビルドに必要なパッケージのインストールと-spdylayのビルド&#34;&gt;ビルドに必要なパッケージのインストールと、spdylayのビルド&lt;/h3&gt;

&lt;pre&gt;&lt;code&gt;mkdir -p /usr/local/src
cd /usr/local/src
git clone https://github.com/tatsuhiro-t/spdylay #spdylayが必要なため

#必要なパッケージをインストール
sudo apt-get install autoconf automake autotools-dev libtool pkg-config zlib1g-dev libcunit1-dev libssl-dev libxml2-dev libevent-dev
cd spdylay/
autoreconf -i
automake
autoconf
./configure
make
make install
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;nghttp2をビルド&#34;&gt;nghttp2をビルド&lt;/h3&gt;

&lt;pre&gt;&lt;code&gt;cd /usr/local/src/
git clone https://github.com/nghttp2/nghttp2     #h2loadはnghttp2に同梱されているため
cd nghttp2/
autoreconf -i
automake
autoconf
./configure
make
make install
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;これでh2loadが利用可能になりましたが、&lt;code&gt;h2load&lt;/code&gt;コマンドを叩くと以下のエラーが起きました。&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;h2load: error while loading shared libraries: libspdylay.so.7: cannot open shared object file: No such file or directory
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;これを解決するには、&lt;code&gt;ldconfig&lt;/code&gt;コマンドを実行します。&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;ldconfig
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;インストール完了&#34;&gt;インストール完了&lt;/h3&gt;

&lt;pre&gt;&lt;code&gt;h2load -n100000 -c100 -m100 https://meganii.com/
starting benchmark...
spawning thread #0: 100 total client(s). 100000 total requests
TLS Protocol: TLSv1.2
Cipher: ECDHE-RSA-AES128-GCM-SHA256
Application protocol: h2
progress: 10% done
progress: 20% done
progress: 30% done
progress: 40% done
progress: 50% done
progress: 60% done
progress: 70% done
progress: 80% done
progress: 90% done
progress: 100% done

finished in 1057.51s, 94.56 req/s, 5.97MB/s
requests: 100000 total, 100000 started, 100000 done, 98534 succeeded, 1466 failed, 0 errored, 0 timeout
status codes: 98534 2xx, 0 3xx, 0 4xx, 1466 5xx
traffic: 6.16GB (6617383053) total, 1.56MB (1634036) headers (space savings 90.77%), 6.16GB (6610382710) data
                     min         max         mean         sd        +/- sd
time for request:      2.05s     305.07s      99.06s      53.36s    68.82%
time for connect:   209.74ms       1.72s       1.23s    360.78ms    54.00%
time to 1st byte:      1.02s       3.18s       1.70s    565.46ms    55.00%
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;まとめ&#34;&gt;まとめ&lt;/h2&gt;

&lt;p&gt;ただ単純にhttp/2プロトコルに対応したベンチマークツールを入れたいだけだったのに、かなり苦労しました。&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>